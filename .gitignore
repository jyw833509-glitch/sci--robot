# 配置（含密钥，切勿提交）
config.yaml
config.json
.env

# 运行产物（feed.json 需提交供同事拉取，故单独放行；其余忽略）
data/*
!data/feed.json
logs/
reports/
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
.venv*/
venv/
env/
*.egg-info/
.pytest_cache/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# WorkBuddy 内部工作记忆（含构建笔记，不应进入公开仓库）
.workbuddy/

# 发给同事的打包产物（本地分发用，不进仓库）
dist/

# PyInstaller 构建临时文件
build/
_pyi_build/
pyi_build/
pyi_dist/
*.spec
